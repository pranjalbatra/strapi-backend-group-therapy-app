{
  "routes": [
    {
      "method": "GET",
      "path": "/patients",
      "handler": "patients.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/patients/count",
      "handler": "patients.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/patients/:id",
      "handler": "patients.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/patients",
      "handler": "patients.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/patients/:id",
      "handler": "patients.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/patients/:id",
      "handler": "patients.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
