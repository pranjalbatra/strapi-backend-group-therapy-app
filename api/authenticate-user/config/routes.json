{
    "routes": 
    [
        {
            "method": "POST",
            "path": "/authenticate-user",
            "handler": "authenticate-user.authUser",
            "config": {
            "policies": ["plugins::users-permissions.userValidation"]
            }
        }

    ]
}